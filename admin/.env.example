# Log level for the logger wrapper
# Options: trace, debug, info, warn, error
PUBLIC_LOG_LEVEL=warn

# Backend API base URL (only needed if not using Vite proxy)
# PUBLIC_API_BASE_URL=http://localhost:8000
